# AI 协作指南

本文档为在本仓库中工作的 AI 助手提供项目上下文与操作约定。

## 项目简介

光学头电源控制系统：基于 **PyQt5** 的桌面应用，用于多路电源（长条电源 / 方形电源）的集中控制、数据采集、实时监测与 TCP 远程控制。

- **主入口**：`gxtdy.py`
- **核心逻辑**：`Projects/电源控制/`（`UpperPC.py`、`长条电源控制.py`、`方形电源控制.py`、`TCPServer.py` 等）
- **协议文档**：`TCP远程控制协议.md`（修改 TCP 相关功能时须同步更新）
- **用户文档**：`README.md`

## 开发环境

本项目在 **Conda** 环境下开发与运行，请勿假设系统全局 Python 或 venv。

| 项 | 值 |
|---|---|
| Conda 环境名 | `main_env` |
| Python 路径 | `d:\miniconda\envs\main_env\python.exe` |
| 激活命令 | `conda activate main_env` |
| 运行主程序 | `python gxtdy.py` |
| 操作系统 | Windows 10/11 |

运行脚本、安装依赖或调试前，应使用上述 Conda 环境中的 Python。主要依赖：PyQt5、pandas、bitstring、pyserial。

## Git 提交规范

提交信息采用 **Conventional Commits** 风格，格式如下：

```
Type: 简短摘要（一行，说明做了什么）

详细说明正文（换行后写，可多行，说明原因、影响范围、注意事项等）
```

### Type 类型

| Type | 用途 |
|---|---|
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `docs` | 仅文档变更 |
| `refactor` | 重构（不改变外部行为） |
| `style` | 代码格式调整（不影响逻辑） |
| `chore` | 构建、配置、杂项 |

### 示例

```
feat: 添加顺序上电和下电功能，支持在电源设置中配置设备顺序

优化电源控制界面，用户可在电源设置中自定义设备上电/下电顺序，
避免多路电源同时开关带来的冲击电流问题。
```

```
fix: 修正串口扫描函数，增加方形电源串口识别

修正了不能正常识别方形电源串口的 bug，并修复数据采集部分的 datetime 引用错误。
```

**注意**：仅在用户明确要求时才创建 git 提交，不要主动提交。

## 编码约定

- **语言**：界面与注释以中文为主，保持与现有代码一致。
- **文件编码**：`# -*- coding: utf-8 -*-`；读写文本文件使用 `encoding='UTF-8'`。
- **许可证**：源文件头部保留 MPL 2.0 许可证声明（参考 `gxtdy.py`）。
- **最小改动**：只改与任务相关的代码，不重构、不"顺手"优化无关部分。
- **UI 文件**：`.ui` 由 Qt Designer 编辑；对应的 `Ui_*.py` 为生成文件，优先改 `.ui` 或业务逻辑 `.py`。
- **信号槽**：PyQt5 信号命名沿用现有风格（如 `sigInfo`、`volatge_signal`）。

## 配置与敏感文件

| 文件 | 说明 |
|---|---|
| `Auto_config.ini` | TCP 地址/端口等全局配置（默认端口 `10002`） |
| `power_config.json` | 本地电源串口与参数配置，**已在 `.gitignore` 中**，不要提交 |
| `电源采集数据/` | 运行时采集的 CSV 数据目录，不要提交 |

修改配置相关逻辑时，注意区分「代码默认值」与「用户本地配置」，避免硬编码覆盖用户设置。

## 硬件与安全

- 本系统直接控制真实电源设备，修改输出、拉偏、顺序上下电等逻辑时须谨慎。
- 电流限制（`current_limit`）等安全参数不可随意放宽。
- 串口操作涉及多线程（`threading` / `QThread`），注意线程安全，参考 `TCPServer.py` 现有模式。
- 性能测试前后须确保数据采集已停止，否则串口返回数据可能错乱（见 `使用方法.txt`）。

## 常见任务指引

| 任务 | 优先查看 |
|---|---|
| 修改 TCP 远程命令 | `TCPServer.py` + `TCP远程控制协议.md` |
| 调整电源参数/串口 | `power_settings.py`、`power_config.json` 结构 |
| 长条电源驱动 | `psw_xx_xx.py`、`长条电源控制.py` |
| 方形电源驱动 | `gpd3303s.py`、`方形电源控制.py` |
| 数据采集与绘图 | `MyPlot.py`、`动态刷新.py` |
| 工具函数 | `Projects/电源控制/tool.py` |

## 回复语言

与用户交流时使用**简体中文**。
